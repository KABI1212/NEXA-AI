# NEXA AI Directory Specification: `frontend/src/shared/hooks`

## 1. Purpose
Global React custom hooks.

## 2. Expected Files
- `useTheme.ts (Swaps dark/light styles)`
- `useDebounce.ts (Filters keystroke triggers)`

## 3. Responsibilities
Expose shared lifecycle events and state helpers.

## 4. Dependencies
- `react`
