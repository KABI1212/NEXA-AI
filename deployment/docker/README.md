# NEXA AI Directory Specification: `deployment/docker`

## 1. Purpose
Docker Compose configurations for backend and databases.

## 2. Expected Files
- `docker-compose.dev.yml (Local database ports mapping)`
- `docker-compose.prod.yml (Nginx + production stacks config)`

## 3. Responsibilities
Configure database compose ports and orchestrate production containers.

## 4. Dependencies
- None
